diff --git a/src/signals.c b/src/signals.c
index 5db61d5..cbe4907 100644
--- a/src/signals.c
+++ b/src/signals.c
@@ -166,7 +166,823 @@ static const JSCFunctionListEntry tjs_signal_funcs[] = {
     TJS_CFUNC_DEF("signal", 2, tjs_signal),
 };
 
+#include <errno.h>
+#include <fcntl.h>
+#include <signal.h>
+#include <sys/stat.h>
+#if !defined(_MSC_VER)
+#include <unistd.h>
+#endif
+#ifndef _WIN32
+#include <dlfcn.h>
+#endif
+#if defined(_WIN32)
+#include <io.h>  /* _S_IREAD _S_IWRITE */
+#ifndef S_IRUSR
+#define S_IRUSR _S_IREAD
+#endif
+#ifndef S_IWUSR
+#define S_IWUSR _S_IWRITE
+#endif
+#endif
+#if defined(_MSC_VER) && (!defined(F_OK) || !defined(R_OK) || !defined(W_OK) || !defined(X_OK))
+#error "clode: MSVC lacks <unistd.h>, so F_OK/R_OK/W_OK/X_OK must come from libuv (uv/win.h, via private.h -> uv.h). They did not. node reports all four on Windows (node_constants.cc guards each with #ifdef, having pulled uv.h in through node_internals.h), so this engine must too -- failing loudly beats shipping a Windows fs.constants four keys short of node's."
+#endif
+#if defined(_WIN32) && !defined(WSAEINTR)
+#error "clode: the 58 WSA* errno names must come from <winsock2.h>, pulled in by libuv (uv/win.h, via private.h -> uv.h). WSAEINTR is not defined, so every one of them just compiled out and this engine would report a Windows os.constants.errno of 79 keys where node reports 137 -- silently, because a missing constant does not throw. That gap already shipped once (it was invisible until a --check ran on a Windows host), so it fails the build instead of coming back. _WIN32 and not _MSC_VER: mingw has no winsock2 of its own either."
+#endif
+#define CLODE_CONSTANTS_ABI 1
+#define CLODE_ABI_MARKER "clode-constants-abi:1"
+
 void tjs__mod_signals_init(JSContext *ctx, JSValue ns) {
+
+    /* CLODE (generated by scripts/gen-node-constants.mjs — do not hand-edit).
+     *
+     * Report this engine's OWN constants, read from its own headers, as
+     * globalThis.__tjs_constants. The node-shim consumes these and never guesses.
+     *
+     * This replaces three hand-written tables that were each a guess about a
+     * platform the author was not on: a darwin-shaped signal table, a darwin-shaped
+     * errno table, and `_isDarwin ? {...} : {...}` for fs.constants. The last one
+     * was measurably wrong — on NetBSD, 8 of 11 O_* values were Linux's, and
+     * nothing noticed, because a wrong constant does not throw.
+     *
+     * Names come from node's own list (node_constants.cc); values come from
+     * #ifdef against the headers THIS engine compiled with, so a target nobody has
+     * tabulated is still exact. A name libuv defines as an enum is emitted
+     * unguarded — #ifdef cannot see an enum and would drop it silently, which is
+     * the failure this design exists to end; if it is truly absent the build fails
+     * loudly instead.
+     *
+     * __tjs_abi is the handshake: the shim requires a minimum, and clode's build
+     * refuses an engine that predates it rather than blobulating a quaude that is subtly
+     * wrong at runtime.
+     */
+    typedef struct { const char *name; int64_t val; } clode_kv;
+#define CLODE_K(n, v) { #n, (int64_t)(v) }
+/* Storage class for the tables below. Cosmopolitan libc resolves SIG*, E*, O_*
+ * and RTLD_* at RUNTIME -- one APE binary runs on Linux, macOS, Windows and the
+ * BSDs, where the numbers differ -- so those initializers are not constant
+ * expressions and a 'static const' table cannot be emitted into .rodata.
+ * Dropping const lets cosmocc runtime-initialize it. This USED to live in
+ * patches/libtjs-cosmo.patch as a hand-written hunk whose context was the text
+ * THIS generator emits; regenerating broke it (f8546da renamed clode_sig_list
+ * to clode_sig_kv and the cosmo leg went red for 13 commits, unnoticed because
+ * no CI ran in between). Generated code owns its own storage class now, so all
+ * five tables are covered and there is no context to rot. */
+#ifdef __COSMOPOLITAN__
+#define CLODE_KV_TABLE static clode_kv
+#else
+#define CLODE_KV_TABLE static const clode_kv
+#endif
+    CLODE_KV_TABLE clode_fs_kv[] = {
+        CLODE_K(UV_FS_SYMLINK_DIR, UV_FS_SYMLINK_DIR),
+        CLODE_K(UV_FS_SYMLINK_JUNCTION, UV_FS_SYMLINK_JUNCTION),
+#ifdef O_RDONLY
+        CLODE_K(O_RDONLY, O_RDONLY),
+#endif
+#ifdef O_WRONLY
+        CLODE_K(O_WRONLY, O_WRONLY),
+#endif
+#ifdef O_RDWR
+        CLODE_K(O_RDWR, O_RDWR),
+#endif
+        CLODE_K(UV_DIRENT_UNKNOWN, UV_DIRENT_UNKNOWN),
+        CLODE_K(UV_DIRENT_FILE, UV_DIRENT_FILE),
+        CLODE_K(UV_DIRENT_DIR, UV_DIRENT_DIR),
+        CLODE_K(UV_DIRENT_LINK, UV_DIRENT_LINK),
+        CLODE_K(UV_DIRENT_FIFO, UV_DIRENT_FIFO),
+        CLODE_K(UV_DIRENT_SOCKET, UV_DIRENT_SOCKET),
+        CLODE_K(UV_DIRENT_CHAR, UV_DIRENT_CHAR),
+        CLODE_K(UV_DIRENT_BLOCK, UV_DIRENT_BLOCK),
+#ifdef S_IFMT
+        CLODE_K(S_IFMT, S_IFMT),
+#endif
+#ifdef S_IFREG
+        CLODE_K(S_IFREG, S_IFREG),
+#endif
+#ifdef S_IFDIR
+        CLODE_K(S_IFDIR, S_IFDIR),
+#endif
+#ifdef S_IFCHR
+        CLODE_K(S_IFCHR, S_IFCHR),
+#endif
+#ifdef S_IFBLK
+        CLODE_K(S_IFBLK, S_IFBLK),
+#endif
+#ifdef S_IFIFO
+        CLODE_K(S_IFIFO, S_IFIFO),
+#endif
+#ifdef S_IFLNK
+        CLODE_K(S_IFLNK, S_IFLNK),
+#endif
+#ifdef S_IFSOCK
+        CLODE_K(S_IFSOCK, S_IFSOCK),
+#endif
+#ifdef O_CREAT
+        CLODE_K(O_CREAT, O_CREAT),
+#endif
+#ifdef O_EXCL
+        CLODE_K(O_EXCL, O_EXCL),
+#endif
+#ifdef UV_FS_O_FILEMAP
+        CLODE_K(UV_FS_O_FILEMAP, UV_FS_O_FILEMAP),
+#else
+        CLODE_K(UV_FS_O_FILEMAP, 0),
+#endif
+#ifdef UV_FS_O_TEMPORARY
+        CLODE_K(UV_FS_O_TEMPORARY, UV_FS_O_TEMPORARY),
+#else
+        CLODE_K(UV_FS_O_TEMPORARY, 0),
+#endif
+#ifdef UV_FS_O_SHORT_LIVED
+        CLODE_K(UV_FS_O_SHORT_LIVED, UV_FS_O_SHORT_LIVED),
+#else
+        CLODE_K(UV_FS_O_SHORT_LIVED, 0),
+#endif
+#ifdef UV_FS_O_SEQUENTIAL
+        CLODE_K(UV_FS_O_SEQUENTIAL, UV_FS_O_SEQUENTIAL),
+#else
+        CLODE_K(UV_FS_O_SEQUENTIAL, 0),
+#endif
+#ifdef UV_FS_O_RANDOM
+        CLODE_K(UV_FS_O_RANDOM, UV_FS_O_RANDOM),
+#else
+        CLODE_K(UV_FS_O_RANDOM, 0),
+#endif
+#ifdef O_NOCTTY
+        CLODE_K(O_NOCTTY, O_NOCTTY),
+#endif
+#ifdef O_TRUNC
+        CLODE_K(O_TRUNC, O_TRUNC),
+#endif
+#ifdef O_APPEND
+        CLODE_K(O_APPEND, O_APPEND),
+#endif
+#ifdef O_DIRECTORY
+        CLODE_K(O_DIRECTORY, O_DIRECTORY),
+#endif
+#ifdef O_NOATIME
+        CLODE_K(O_NOATIME, O_NOATIME),
+#endif
+#ifdef O_NOFOLLOW
+        CLODE_K(O_NOFOLLOW, O_NOFOLLOW),
+#endif
+#ifdef O_SYNC
+        CLODE_K(O_SYNC, O_SYNC),
+#endif
+#ifdef O_DSYNC
+        CLODE_K(O_DSYNC, O_DSYNC),
+#endif
+#ifdef O_SYMLINK
+        CLODE_K(O_SYMLINK, O_SYMLINK),
+#endif
+#ifdef O_DIRECT
+        CLODE_K(O_DIRECT, O_DIRECT),
+#endif
+#ifdef O_NONBLOCK
+        CLODE_K(O_NONBLOCK, O_NONBLOCK),
+#endif
+#ifdef S_IRWXU
+        CLODE_K(S_IRWXU, S_IRWXU),
+#endif
+#ifdef S_IRUSR
+        CLODE_K(S_IRUSR, S_IRUSR),
+#endif
+#ifdef S_IWUSR
+        CLODE_K(S_IWUSR, S_IWUSR),
+#endif
+#ifdef S_IXUSR
+        CLODE_K(S_IXUSR, S_IXUSR),
+#endif
+#ifdef S_IRWXG
+        CLODE_K(S_IRWXG, S_IRWXG),
+#endif
+#ifdef S_IRGRP
+        CLODE_K(S_IRGRP, S_IRGRP),
+#endif
+#ifdef S_IWGRP
+        CLODE_K(S_IWGRP, S_IWGRP),
+#endif
+#ifdef S_IXGRP
+        CLODE_K(S_IXGRP, S_IXGRP),
+#endif
+#ifdef S_IRWXO
+        CLODE_K(S_IRWXO, S_IRWXO),
+#endif
+#ifdef S_IROTH
+        CLODE_K(S_IROTH, S_IROTH),
+#endif
+#ifdef S_IWOTH
+        CLODE_K(S_IWOTH, S_IWOTH),
+#endif
+#ifdef S_IXOTH
+        CLODE_K(S_IXOTH, S_IXOTH),
+#endif
+#ifdef F_OK
+        CLODE_K(F_OK, F_OK),
+#endif
+#ifdef R_OK
+        CLODE_K(R_OK, R_OK),
+#endif
+#ifdef W_OK
+        CLODE_K(W_OK, W_OK),
+#endif
+#ifdef X_OK
+        CLODE_K(X_OK, X_OK),
+#endif
+        CLODE_K(UV_FS_COPYFILE_EXCL, UV_FS_COPYFILE_EXCL),
+        CLODE_K(COPYFILE_EXCL, UV_FS_COPYFILE_EXCL),
+        CLODE_K(UV_FS_COPYFILE_FICLONE, UV_FS_COPYFILE_FICLONE),
+        CLODE_K(COPYFILE_FICLONE, UV_FS_COPYFILE_FICLONE),
+        CLODE_K(UV_FS_COPYFILE_FICLONE_FORCE, UV_FS_COPYFILE_FICLONE_FORCE),
+        CLODE_K(COPYFILE_FICLONE_FORCE, UV_FS_COPYFILE_FICLONE_FORCE),
+        { NULL, 0 },   /* terminator: keeps an all-guarded-out table legal */
+    };
+    CLODE_KV_TABLE clode_sig_kv[] = {
+#ifdef SIGHUP
+        CLODE_K(SIGHUP, SIGHUP),
+#endif
+#ifdef SIGINT
+        CLODE_K(SIGINT, SIGINT),
+#endif
+#ifdef SIGQUIT
+        CLODE_K(SIGQUIT, SIGQUIT),
+#endif
+#ifdef SIGILL
+        CLODE_K(SIGILL, SIGILL),
+#endif
+#ifdef SIGTRAP
+        CLODE_K(SIGTRAP, SIGTRAP),
+#endif
+#ifdef SIGABRT
+        CLODE_K(SIGABRT, SIGABRT),
+#endif
+#ifdef SIGIOT
+        CLODE_K(SIGIOT, SIGIOT),
+#endif
+#ifdef SIGBUS
+        CLODE_K(SIGBUS, SIGBUS),
+#endif
+#ifdef SIGFPE
+        CLODE_K(SIGFPE, SIGFPE),
+#endif
+#ifdef SIGKILL
+        CLODE_K(SIGKILL, SIGKILL),
+#endif
+#ifdef SIGUSR1
+        CLODE_K(SIGUSR1, SIGUSR1),
+#endif
+#ifdef SIGSEGV
+        CLODE_K(SIGSEGV, SIGSEGV),
+#endif
+#ifdef SIGUSR2
+        CLODE_K(SIGUSR2, SIGUSR2),
+#endif
+#ifdef SIGPIPE
+        CLODE_K(SIGPIPE, SIGPIPE),
+#endif
+#ifdef SIGALRM
+        CLODE_K(SIGALRM, SIGALRM),
+#endif
+#ifdef SIGTERM
+        CLODE_K(SIGTERM, SIGTERM),
+#endif
+#ifdef SIGCHLD
+        CLODE_K(SIGCHLD, SIGCHLD),
+#endif
+#ifdef SIGSTKFLT
+        CLODE_K(SIGSTKFLT, SIGSTKFLT),
+#endif
+#ifdef SIGCONT
+        CLODE_K(SIGCONT, SIGCONT),
+#endif
+#ifdef SIGSTOP
+        CLODE_K(SIGSTOP, SIGSTOP),
+#endif
+#ifdef SIGTSTP
+        CLODE_K(SIGTSTP, SIGTSTP),
+#endif
+#ifdef SIGBREAK
+        CLODE_K(SIGBREAK, SIGBREAK),
+#endif
+#ifdef SIGTTIN
+        CLODE_K(SIGTTIN, SIGTTIN),
+#endif
+#ifdef SIGTTOU
+        CLODE_K(SIGTTOU, SIGTTOU),
+#endif
+#ifdef SIGURG
+        CLODE_K(SIGURG, SIGURG),
+#endif
+#ifdef SIGXCPU
+        CLODE_K(SIGXCPU, SIGXCPU),
+#endif
+#ifdef SIGXFSZ
+        CLODE_K(SIGXFSZ, SIGXFSZ),
+#endif
+#ifdef SIGVTALRM
+        CLODE_K(SIGVTALRM, SIGVTALRM),
+#endif
+#ifdef SIGPROF
+        CLODE_K(SIGPROF, SIGPROF),
+#endif
+#ifdef SIGWINCH
+        CLODE_K(SIGWINCH, SIGWINCH),
+#endif
+#ifdef SIGIO
+        CLODE_K(SIGIO, SIGIO),
+#endif
+#ifdef SIGPOLL
+        CLODE_K(SIGPOLL, SIGPOLL),
+#endif
+#ifdef SIGLOST
+        CLODE_K(SIGLOST, SIGLOST),
+#endif
+#ifdef SIGPWR
+        CLODE_K(SIGPWR, SIGPWR),
+#endif
+#ifdef SIGINFO
+        CLODE_K(SIGINFO, SIGINFO),
+#endif
+#ifdef SIGSYS
+        CLODE_K(SIGSYS, SIGSYS),
+#endif
+#ifdef SIGUNUSED
+        CLODE_K(SIGUNUSED, SIGUNUSED),
+#endif
+        { NULL, 0 },   /* terminator: keeps an all-guarded-out table legal */
+    };
+    CLODE_KV_TABLE clode_errno_kv[] = {
+#ifdef E2BIG
+        CLODE_K(E2BIG, E2BIG),
+#endif
+#ifdef EACCES
+        CLODE_K(EACCES, EACCES),
+#endif
+#ifdef EADDRINUSE
+        CLODE_K(EADDRINUSE, EADDRINUSE),
+#endif
+#ifdef EADDRNOTAVAIL
+        CLODE_K(EADDRNOTAVAIL, EADDRNOTAVAIL),
+#endif
+#ifdef EAFNOSUPPORT
+        CLODE_K(EAFNOSUPPORT, EAFNOSUPPORT),
+#endif
+#ifdef EAGAIN
+        CLODE_K(EAGAIN, EAGAIN),
+#endif
+#ifdef EALREADY
+        CLODE_K(EALREADY, EALREADY),
+#endif
+#ifdef EBADF
+        CLODE_K(EBADF, EBADF),
+#endif
+#ifdef EBADMSG
+        CLODE_K(EBADMSG, EBADMSG),
+#endif
+#ifdef EBUSY
+        CLODE_K(EBUSY, EBUSY),
+#endif
+#ifdef ECANCELED
+        CLODE_K(ECANCELED, ECANCELED),
+#endif
+#ifdef ECHILD
+        CLODE_K(ECHILD, ECHILD),
+#endif
+#ifdef ECONNABORTED
+        CLODE_K(ECONNABORTED, ECONNABORTED),
+#endif
+#ifdef ECONNREFUSED
+        CLODE_K(ECONNREFUSED, ECONNREFUSED),
+#endif
+#ifdef ECONNRESET
+        CLODE_K(ECONNRESET, ECONNRESET),
+#endif
+#ifdef EDEADLK
+        CLODE_K(EDEADLK, EDEADLK),
+#endif
+#ifdef EDESTADDRREQ
+        CLODE_K(EDESTADDRREQ, EDESTADDRREQ),
+#endif
+#ifdef EDOM
+        CLODE_K(EDOM, EDOM),
+#endif
+#ifdef EDQUOT
+        CLODE_K(EDQUOT, EDQUOT),
+#endif
+#ifdef EEXIST
+        CLODE_K(EEXIST, EEXIST),
+#endif
+#ifdef EFAULT
+        CLODE_K(EFAULT, EFAULT),
+#endif
+#ifdef EFBIG
+        CLODE_K(EFBIG, EFBIG),
+#endif
+#ifdef EHOSTUNREACH
+        CLODE_K(EHOSTUNREACH, EHOSTUNREACH),
+#endif
+#ifdef EIDRM
+        CLODE_K(EIDRM, EIDRM),
+#endif
+#ifdef EILSEQ
+        CLODE_K(EILSEQ, EILSEQ),
+#endif
+#ifdef EINPROGRESS
+        CLODE_K(EINPROGRESS, EINPROGRESS),
+#endif
+#ifdef EINTR
+        CLODE_K(EINTR, EINTR),
+#endif
+#ifdef EINVAL
+        CLODE_K(EINVAL, EINVAL),
+#endif
+#ifdef EIO
+        CLODE_K(EIO, EIO),
+#endif
+#ifdef EISCONN
+        CLODE_K(EISCONN, EISCONN),
+#endif
+#ifdef EISDIR
+        CLODE_K(EISDIR, EISDIR),
+#endif
+#ifdef ELOOP
+        CLODE_K(ELOOP, ELOOP),
+#endif
+#ifdef EMFILE
+        CLODE_K(EMFILE, EMFILE),
+#endif
+#ifdef EMLINK
+        CLODE_K(EMLINK, EMLINK),
+#endif
+#ifdef EMSGSIZE
+        CLODE_K(EMSGSIZE, EMSGSIZE),
+#endif
+#ifdef EMULTIHOP
+        CLODE_K(EMULTIHOP, EMULTIHOP),
+#endif
+#ifdef ENAMETOOLONG
+        CLODE_K(ENAMETOOLONG, ENAMETOOLONG),
+#endif
+#ifdef ENETDOWN
+        CLODE_K(ENETDOWN, ENETDOWN),
+#endif
+#ifdef ENETRESET
+        CLODE_K(ENETRESET, ENETRESET),
+#endif
+#ifdef ENETUNREACH
+        CLODE_K(ENETUNREACH, ENETUNREACH),
+#endif
+#ifdef ENFILE
+        CLODE_K(ENFILE, ENFILE),
+#endif
+#ifdef ENOBUFS
+        CLODE_K(ENOBUFS, ENOBUFS),
+#endif
+#ifdef ENODATA
+        CLODE_K(ENODATA, ENODATA),
+#endif
+#ifdef ENODEV
+        CLODE_K(ENODEV, ENODEV),
+#endif
+#ifdef ENOENT
+        CLODE_K(ENOENT, ENOENT),
+#endif
+#ifdef ENOEXEC
+        CLODE_K(ENOEXEC, ENOEXEC),
+#endif
+#ifdef ENOLCK
+        CLODE_K(ENOLCK, ENOLCK),
+#endif
+#ifdef ENOLINK
+        CLODE_K(ENOLINK, ENOLINK),
+#endif
+#ifdef ENOMEM
+        CLODE_K(ENOMEM, ENOMEM),
+#endif
+#ifdef ENOMSG
+        CLODE_K(ENOMSG, ENOMSG),
+#endif
+#ifdef ENOPROTOOPT
+        CLODE_K(ENOPROTOOPT, ENOPROTOOPT),
+#endif
+#ifdef ENOSPC
+        CLODE_K(ENOSPC, ENOSPC),
+#endif
+#ifdef ENOSR
+        CLODE_K(ENOSR, ENOSR),
+#endif
+#ifdef ENOSTR
+        CLODE_K(ENOSTR, ENOSTR),
+#endif
+#ifdef ENOSYS
+        CLODE_K(ENOSYS, ENOSYS),
+#endif
+#ifdef ENOTCONN
+        CLODE_K(ENOTCONN, ENOTCONN),
+#endif
+#ifdef ENOTDIR
+        CLODE_K(ENOTDIR, ENOTDIR),
+#endif
+#ifdef ENOTEMPTY
+        CLODE_K(ENOTEMPTY, ENOTEMPTY),
+#endif
+#ifdef ENOTSOCK
+        CLODE_K(ENOTSOCK, ENOTSOCK),
+#endif
+#ifdef ENOTSUP
+        CLODE_K(ENOTSUP, ENOTSUP),
+#endif
+#ifdef ENOTTY
+        CLODE_K(ENOTTY, ENOTTY),
+#endif
+#ifdef ENXIO
+        CLODE_K(ENXIO, ENXIO),
+#endif
+#ifdef EOPNOTSUPP
+        CLODE_K(EOPNOTSUPP, EOPNOTSUPP),
+#endif
+#ifdef EOVERFLOW
+        CLODE_K(EOVERFLOW, EOVERFLOW),
+#endif
+#ifdef EPERM
+        CLODE_K(EPERM, EPERM),
+#endif
+#ifdef EPIPE
+        CLODE_K(EPIPE, EPIPE),
+#endif
+#ifdef EPROTO
+        CLODE_K(EPROTO, EPROTO),
+#endif
+#ifdef EPROTONOSUPPORT
+        CLODE_K(EPROTONOSUPPORT, EPROTONOSUPPORT),
+#endif
+#ifdef EPROTOTYPE
+        CLODE_K(EPROTOTYPE, EPROTOTYPE),
+#endif
+#ifdef ERANGE
+        CLODE_K(ERANGE, ERANGE),
+#endif
+#ifdef EROFS
+        CLODE_K(EROFS, EROFS),
+#endif
+#ifdef ESPIPE
+        CLODE_K(ESPIPE, ESPIPE),
+#endif
+#ifdef ESRCH
+        CLODE_K(ESRCH, ESRCH),
+#endif
+#ifdef ESTALE
+        CLODE_K(ESTALE, ESTALE),
+#endif
+#ifdef ETIME
+        CLODE_K(ETIME, ETIME),
+#endif
+#ifdef ETIMEDOUT
+        CLODE_K(ETIMEDOUT, ETIMEDOUT),
+#endif
+#ifdef ETXTBSY
+        CLODE_K(ETXTBSY, ETXTBSY),
+#endif
+#ifdef EWOULDBLOCK
+        CLODE_K(EWOULDBLOCK, EWOULDBLOCK),
+#endif
+#ifdef EXDEV
+        CLODE_K(EXDEV, EXDEV),
+#endif
+#ifdef WSAEINTR
+        CLODE_K(WSAEINTR, WSAEINTR),
+#endif
+#ifdef WSAEBADF
+        CLODE_K(WSAEBADF, WSAEBADF),
+#endif
+#ifdef WSAEACCES
+        CLODE_K(WSAEACCES, WSAEACCES),
+#endif
+#ifdef WSAEFAULT
+        CLODE_K(WSAEFAULT, WSAEFAULT),
+#endif
+#ifdef WSAEINVAL
+        CLODE_K(WSAEINVAL, WSAEINVAL),
+#endif
+#ifdef WSAEMFILE
+        CLODE_K(WSAEMFILE, WSAEMFILE),
+#endif
+#ifdef WSAEWOULDBLOCK
+        CLODE_K(WSAEWOULDBLOCK, WSAEWOULDBLOCK),
+#endif
+#ifdef WSAEINPROGRESS
+        CLODE_K(WSAEINPROGRESS, WSAEINPROGRESS),
+#endif
+#ifdef WSAEALREADY
+        CLODE_K(WSAEALREADY, WSAEALREADY),
+#endif
+#ifdef WSAENOTSOCK
+        CLODE_K(WSAENOTSOCK, WSAENOTSOCK),
+#endif
+#ifdef WSAEDESTADDRREQ
+        CLODE_K(WSAEDESTADDRREQ, WSAEDESTADDRREQ),
+#endif
+#ifdef WSAEMSGSIZE
+        CLODE_K(WSAEMSGSIZE, WSAEMSGSIZE),
+#endif
+#ifdef WSAEPROTOTYPE
+        CLODE_K(WSAEPROTOTYPE, WSAEPROTOTYPE),
+#endif
+#ifdef WSAENOPROTOOPT
+        CLODE_K(WSAENOPROTOOPT, WSAENOPROTOOPT),
+#endif
+#ifdef WSAEPROTONOSUPPORT
+        CLODE_K(WSAEPROTONOSUPPORT, WSAEPROTONOSUPPORT),
+#endif
+#ifdef WSAESOCKTNOSUPPORT
+        CLODE_K(WSAESOCKTNOSUPPORT, WSAESOCKTNOSUPPORT),
+#endif
+#ifdef WSAEOPNOTSUPP
+        CLODE_K(WSAEOPNOTSUPP, WSAEOPNOTSUPP),
+#endif
+#ifdef WSAEPFNOSUPPORT
+        CLODE_K(WSAEPFNOSUPPORT, WSAEPFNOSUPPORT),
+#endif
+#ifdef WSAEAFNOSUPPORT
+        CLODE_K(WSAEAFNOSUPPORT, WSAEAFNOSUPPORT),
+#endif
+#ifdef WSAEADDRINUSE
+        CLODE_K(WSAEADDRINUSE, WSAEADDRINUSE),
+#endif
+#ifdef WSAEADDRNOTAVAIL
+        CLODE_K(WSAEADDRNOTAVAIL, WSAEADDRNOTAVAIL),
+#endif
+#ifdef WSAENETDOWN
+        CLODE_K(WSAENETDOWN, WSAENETDOWN),
+#endif
+#ifdef WSAENETUNREACH
+        CLODE_K(WSAENETUNREACH, WSAENETUNREACH),
+#endif
+#ifdef WSAENETRESET
+        CLODE_K(WSAENETRESET, WSAENETRESET),
+#endif
+#ifdef WSAECONNABORTED
+        CLODE_K(WSAECONNABORTED, WSAECONNABORTED),
+#endif
+#ifdef WSAECONNRESET
+        CLODE_K(WSAECONNRESET, WSAECONNRESET),
+#endif
+#ifdef WSAENOBUFS
+        CLODE_K(WSAENOBUFS, WSAENOBUFS),
+#endif
+#ifdef WSAEISCONN
+        CLODE_K(WSAEISCONN, WSAEISCONN),
+#endif
+#ifdef WSAENOTCONN
+        CLODE_K(WSAENOTCONN, WSAENOTCONN),
+#endif
+#ifdef WSAESHUTDOWN
+        CLODE_K(WSAESHUTDOWN, WSAESHUTDOWN),
+#endif
+#ifdef WSAETOOMANYREFS
+        CLODE_K(WSAETOOMANYREFS, WSAETOOMANYREFS),
+#endif
+#ifdef WSAETIMEDOUT
+        CLODE_K(WSAETIMEDOUT, WSAETIMEDOUT),
+#endif
+#ifdef WSAECONNREFUSED
+        CLODE_K(WSAECONNREFUSED, WSAECONNREFUSED),
+#endif
+#ifdef WSAELOOP
+        CLODE_K(WSAELOOP, WSAELOOP),
+#endif
+#ifdef WSAENAMETOOLONG
+        CLODE_K(WSAENAMETOOLONG, WSAENAMETOOLONG),
+#endif
+#ifdef WSAEHOSTDOWN
+        CLODE_K(WSAEHOSTDOWN, WSAEHOSTDOWN),
+#endif
+#ifdef WSAEHOSTUNREACH
+        CLODE_K(WSAEHOSTUNREACH, WSAEHOSTUNREACH),
+#endif
+#ifdef WSAENOTEMPTY
+        CLODE_K(WSAENOTEMPTY, WSAENOTEMPTY),
+#endif
+#ifdef WSAEPROCLIM
+        CLODE_K(WSAEPROCLIM, WSAEPROCLIM),
+#endif
+#ifdef WSAEUSERS
+        CLODE_K(WSAEUSERS, WSAEUSERS),
+#endif
+#ifdef WSAEDQUOT
+        CLODE_K(WSAEDQUOT, WSAEDQUOT),
+#endif
+#ifdef WSAESTALE
+        CLODE_K(WSAESTALE, WSAESTALE),
+#endif
+#ifdef WSAEREMOTE
+        CLODE_K(WSAEREMOTE, WSAEREMOTE),
+#endif
+#ifdef WSASYSNOTREADY
+        CLODE_K(WSASYSNOTREADY, WSASYSNOTREADY),
+#endif
+#ifdef WSAVERNOTSUPPORTED
+        CLODE_K(WSAVERNOTSUPPORTED, WSAVERNOTSUPPORTED),
+#endif
+#ifdef WSANOTINITIALISED
+        CLODE_K(WSANOTINITIALISED, WSANOTINITIALISED),
+#endif
+#ifdef WSAEDISCON
+        CLODE_K(WSAEDISCON, WSAEDISCON),
+#endif
+#ifdef WSAENOMORE
+        CLODE_K(WSAENOMORE, WSAENOMORE),
+#endif
+#ifdef WSAECANCELLED
+        CLODE_K(WSAECANCELLED, WSAECANCELLED),
+#endif
+#ifdef WSAEINVALIDPROCTABLE
+        CLODE_K(WSAEINVALIDPROCTABLE, WSAEINVALIDPROCTABLE),
+#endif
+#ifdef WSAEINVALIDPROVIDER
+        CLODE_K(WSAEINVALIDPROVIDER, WSAEINVALIDPROVIDER),
+#endif
+#ifdef WSAEPROVIDERFAILEDINIT
+        CLODE_K(WSAEPROVIDERFAILEDINIT, WSAEPROVIDERFAILEDINIT),
+#endif
+#ifdef WSASYSCALLFAILURE
+        CLODE_K(WSASYSCALLFAILURE, WSASYSCALLFAILURE),
+#endif
+#ifdef WSASERVICE_NOT_FOUND
+        CLODE_K(WSASERVICE_NOT_FOUND, WSASERVICE_NOT_FOUND),
+#endif
+#ifdef WSATYPE_NOT_FOUND
+        CLODE_K(WSATYPE_NOT_FOUND, WSATYPE_NOT_FOUND),
+#endif
+#ifdef WSA_E_NO_MORE
+        CLODE_K(WSA_E_NO_MORE, WSA_E_NO_MORE),
+#endif
+#ifdef WSA_E_CANCELLED
+        CLODE_K(WSA_E_CANCELLED, WSA_E_CANCELLED),
+#endif
+#ifdef WSAEREFUSED
+        CLODE_K(WSAEREFUSED, WSAEREFUSED),
+#endif
+        { NULL, 0 },   /* terminator: keeps an all-guarded-out table legal */
+    };
+    CLODE_KV_TABLE clode_dlopen_kv[] = {
+#ifdef RTLD_LAZY
+        CLODE_K(RTLD_LAZY, RTLD_LAZY),
+#endif
+#ifdef RTLD_NOW
+        CLODE_K(RTLD_NOW, RTLD_NOW),
+#endif
+#ifdef RTLD_GLOBAL
+        CLODE_K(RTLD_GLOBAL, RTLD_GLOBAL),
+#endif
+#ifdef RTLD_LOCAL
+        CLODE_K(RTLD_LOCAL, RTLD_LOCAL),
+#endif
+#ifdef RTLD_DEEPBIND
+        CLODE_K(RTLD_DEEPBIND, RTLD_DEEPBIND),
+#endif
+        { NULL, 0 },   /* terminator: keeps an all-guarded-out table legal */
+    };
+    CLODE_KV_TABLE clode_priority_kv[] = {
+        CLODE_K(PRIORITY_LOW, UV_PRIORITY_LOW),
+        CLODE_K(PRIORITY_BELOW_NORMAL, UV_PRIORITY_BELOW_NORMAL),
+        CLODE_K(PRIORITY_NORMAL, UV_PRIORITY_NORMAL),
+        CLODE_K(PRIORITY_ABOVE_NORMAL, UV_PRIORITY_ABOVE_NORMAL),
+        CLODE_K(PRIORITY_HIGH, UV_PRIORITY_HIGH),
+        CLODE_K(PRIORITY_HIGHEST, UV_PRIORITY_HIGHEST),
+        { NULL, 0 },   /* terminator: keeps an all-guarded-out table legal */
+    };
+#undef CLODE_K
+#undef CLODE_KV_TABLE
+    JSValue clode_c = JS_NewObject(ctx);
+    JS_SetPropertyStr(ctx, clode_c, "abi", JS_NewInt32(ctx, CLODE_CONSTANTS_ABI));
+    /* A DISTINCTIVE literal, so clode's build can verify an engine it cannot run
+     * (a cross-target template) by looking for it in the binary. Sniffing was
+     * rejected before for good reason — probing for "uid"/"gid" gave false
+     * confidence because libc contains those strings anyway — but this token
+     * cannot appear by accident, and setting it as a property guarantees the
+     * literal survives into the binary instead of being optimized away. */
+    JS_SetPropertyStr(ctx, clode_c, "marker", JS_NewString(ctx, CLODE_ABI_MARKER));
+#define CLODE_GROUP(field, arr)                                                       \
+    do {                                                                              \
+        JSValue o_ = JS_NewObject(ctx);                                               \
+        for (size_t i_ = 0; i_ < countof(arr); i_++)                                  \
+            if (arr[i_].name)   /* skip the terminator */                                     \
+                JS_SetPropertyStr(ctx, o_, arr[i_].name, JS_NewInt64(ctx, arr[i_].val));      \
+        JS_SetPropertyStr(ctx, clode_c, field, o_);                                   \
+    } while (0)
+    CLODE_GROUP("fs", clode_fs_kv);
+    CLODE_GROUP("signals", clode_sig_kv);
+    CLODE_GROUP("errno", clode_errno_kv);
+    CLODE_GROUP("dlopen", clode_dlopen_kv);
+    CLODE_GROUP("priority", clode_priority_kv);
+#undef CLODE_GROUP
+    JS_SetPropertyStr(ctx, clode_c, "UV_UDP_REUSEADDR", JS_NewInt32(ctx, UV_UDP_REUSEADDR));
+    JSValue clode_g = JS_GetGlobalObject(ctx);
+    JS_DefinePropertyValueStr(ctx, clode_g, "__tjs_constants", clode_c, JS_PROP_C_W_E);
+    JS_FreeValue(ctx, clode_g);
     JSRuntime *rt = JS_GetRuntime(ctx);
 
     JS_NewClassID(rt, &tjs_signal_handler_class_id);
