{
  "name": "clode",
  "version": "0.1.0",
  "private": true,
  "description": "Run the latest Claude Code under Node on any machine: clode extracts the upstream bundle and runs it under a host Node via a small Bun shim. Install with `npm install -g .`; the runtime deps below are installed automatically on first run for non-npm installs.",
  "bin": {
    "clode": "bin/clode"
  },
  "files": [
    "bin/",
    "libexec/",
    "man/",
    "VERSION"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "sh test/run-all.sh",
    "test:online": "sh test/run-all.sh --online"
  },
  "dependencies": {
    "semver": "^7",
    "string-width": "^8.0.0",
    "strip-ansi": "^7",
    "wrap-ansi": "^10.0.0",
    "ws": "^8",
    "yaml": "^2"
  }
}
